package com.base.android.kotlinbasic.E_05_연산자

/*
   ? -> val a:Int?  -> a 변수를 null 허용선언
   ?: ->  A?:B  A가 null이면 B 실행
   !! -> A !! B A가 null이 아닐때만 B실행, null 이면 예외 발생
   ?. -> A?.length A가 null이면 null, null이 아니면 length
 */